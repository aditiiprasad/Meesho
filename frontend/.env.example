# Copy to frontend/.env.local for local dev, or set in Vercel → Environment Variables.
# Required at build time (Vite bakes this into the bundle).

# Local:
# VITE_API_URL=http://127.0.0.1:8000

# Production (your Render backend URL, no trailing slash):
VITE_API_URL=https://meesho-5iqg.onrender.com
